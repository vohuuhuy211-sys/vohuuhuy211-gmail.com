# AI 协作与交付规则

本文件适用于整个仓库。若子目录存在更具体的 `AGENTS.md`，应同时遵守，冲突时以更严格的规则为准。

## 开发前

1. 每次开发必须对应一个唯一任务编号，并在分支、提交或 Pull Request（拉取请求）中可追溯。
2. 开发前必须读取对应的 `tasks/*.yaml` 任务文件、`docs/PRODUCT_RULES.md` 和 `docs/USER_COLLABORATION_RULES.md`。
3. 先确认业务目标、范围、验收标准、限制和风险；发现遗漏或冲突时应明确记录，不能静默猜测关键业务决定。
4. 不得擅自扩大需求范围。必要但超出范围的工作应列为后续事项。

## 开发与验证

1. 不得用假数据、Mock（模拟）数据、硬编码结果或仅展示界面冒充功能完成；测试专用替身必须清楚标注且不能进入真实业务结果。
2. 不得跳过测试。应运行与变更相称的现有测试，并为新增行为补充必要测试；无法运行时必须给出真实原因和影响。
3. 不得以“已完成”的聊天回复代替真实代码、测试结果和可复核成果。
4. 必须保存测试日志、生成成果及必要执行日志，供 CI（持续集成）和审查复核。
5. 技术说明使用通俗中文；英文技术词首次出现时附中文解释。

## Git 与交付

1. 必须通过独立分支和 Pull Request（拉取请求）交付，不能直接向 `main`、`master` 或其他正式分支提交。
2. 检查未通过时不得合并；Codex 不得自行合并 Pull Request。
3. 交付说明必须列出所有新增、修改、删除的文件。
4. 交付说明必须报告真实测试命令、结果、日志位置、生成成果、未完成内容和已知风险。
5. 使用 `scripts/collect_delivery_context.py` 生成 `delivery-context/`，并由 GitHub Actions（GitHub 自动化工作流）保存为构建产物。
6. 遇到账号登录、GitHub 授权、API（应用程序接口）密钥、验证码或付费操作时立即停止，说明需要用户亲自完成的步骤，不得绕过。

